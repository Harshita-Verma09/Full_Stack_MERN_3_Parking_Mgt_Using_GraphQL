# Frontend — MERN Parking System

Frontend is built with React and Vite.

Run (from `frontend/`):

```bash
npm install
npm run dev
```

Open `http://localhost:5173` in your browser.

Notes

- The app communicates with the backend GraphQL server at `http://localhost:5000` by default.
- Update API endpoints in the frontend if you change backend port or host.
